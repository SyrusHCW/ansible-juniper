- name: Apply configuration
  hosts: core_switches
  gather_facts: True
  connection: local
  tasks:
    - name: Juniper Commit
      junos_config:
        lines:
          - set system host-name "{{ hostname }}"
        timeout: 30
        comment: "Push from Ansible"
        confirm_commit: yes
