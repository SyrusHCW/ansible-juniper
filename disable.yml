- hosts: localhost
  gather_facts: no
  
  vars:
    transit_fw: "AWS-USE1-TRANS-FW1"
  tasks:
  
    - name: print value_to_reuse
      debug:
        var: hostvars['{{transit_fw}}']['panorama']
      with_items:
        - hostvars['{{transit_fw}}']['panorama']


