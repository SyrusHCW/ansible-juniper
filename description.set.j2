{% for host in groups['core_switches'] %}
set system name-server "{{ hostvars[host]['dns-servers'][0] }}"
set system name-server "{{ hostvars[host]['dns-servers'][1] }}"
set system host-name "{{ hostvars[host]['hostname'] }}"
{% endfor %}
